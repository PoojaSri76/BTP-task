_schema-version: "3.2"
ID: vehicleui5
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: vehicleui5-destination-content
  type: com.sap.application.content
  requires:
  - name: vehicleui5-destination-service
    parameters:
      content-target: true
  - name: vehicleui5-repo-host
    parameters:
      service-key:
        name: vehicleui5-repo-host-key
  - name: vehicleui5-uaa
    parameters:
      service-key:
        name: vehicleui5-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: vehicleui5_html_repo_host
          ServiceInstanceName: vehicleui5-html5-service
          ServiceKeyName: vehicleui5-repo-host-key
          sap.cloud.service: vehicleui5
        - Authentication: OAuth2UserTokenExchange
          Name: vehicleui5_uaa
          ServiceInstanceName: vehicleui5-xsuaa-service
          ServiceKeyName: vehicleui5-uaa-key
          sap.cloud.service: vehicleui5
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: vehicleui5-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: vehicleui5-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - vehicleui5.zip
      name: vehicleui5
      target-path: resources/
- name: vehicleui5
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: vehicleui5-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          - Authentication: NoAuthentication
            Name: vehicle_backend
            ProxyType: Internet
            Type: HTTP
            URL: https://1ce06d92trial-dev-vehicle-srv.cfapps.us10-003.hana.ondemand.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: vehicleui5-destination-service
    service-plan: lite
- name: vehicleui5-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: vehicleui5-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-name: vehicleui5-xsuaa-service
    service-plan: application
- name: vehicleui5-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: vehicleui5-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
