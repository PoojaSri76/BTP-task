_schema-version: 3.3.0
ID: expenseClaimManagement
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: expenseClaimManagement-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: expenseClaimManagement-auth
      - name: expenseClaimManagement-db
      - name: expenseClaimManagement-destination

  - name: expenseClaimManagement-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: expenseClaimManagement-db
  - name: expenseClaimManagement
    type: approuter.nodejs
    path: app/router
    parameters:
      keep-existing-routes: true
      disk-quota: 256M
      memory: 256M
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true
      - name: expenseClaimManagement-auth
      - name: expenseClaimManagement-html5-runtime
    provides:
      - name: app-api
        properties:
          app-protocol: ${protocol}
          app-uri: ${default-uri}

  - name: expenseClaimManagement-app-deployer
    type: com.sap.application.content
    path: gen
    build-parameters:
      build-result: app/
      requires:
        - name: expenseClaimManagementapprover
          artifacts:
            - approver.zip
          target-path: app/
        - name: expenseClaimManagementemployee
          artifacts:
            - employee.zip
          target-path: app/
        - name: expenseClaimManagementfinance
          artifacts:
            - finance.zip
          target-path: app/
    requires:
      - name: expenseClaimManagement-auth
      - name: srv-api
      - name: expenseClaimManagement-html5-repo-host
        parameters:
          content-target: true
          config:
            HTML5Runtime_enabled: true
    parameters:
      config:
        destinations:
          - Name: srv-api
            URL: ~{srv-api/srv-url}
            Authentication: NoAuthentication
            Type: HTTP
            ProxyType: Internet
            HTML5.ForwardAuthToken: true
            HTML5.DynamicDestination: true

  - name: expenseClaimManagementapprover
    type: html5
    path: app/approver
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []
  - name: expenseClaimManagementemployee
    type: html5
    path: app/employee
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []
  - name: expenseClaimManagementfinance
    type: html5
    path: app/finance
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []

  - name: expenseClaimManagement-destinations
    type: com.sap.application.content
    requires:
      - name: expenseClaimManagement-auth
        parameters:
          service-key:
            name: expenseClaimManagement-auth-key
      - name: expenseClaimManagement-html5-repo-host
        parameters:
          service-key:
            name: expenseClaimManagement-html5-repo-host-key
      - name: srv-api
      - name: expenseClaimManagement-destination
        parameters:
          content-target: true
    build-parameters:
      no-source: true
    parameters:
      content:
        instance:
          existing_destinations_policy: update
          destinations:
            - Name: expenseClaimManagement-html5-repository
              ServiceInstanceName: expenseClaimManagement-html5-repo-host
              ServiceKeyName: expenseClaimManagement-html5-repo-host-key
              sap.cloud.service: expenseClaimManagement.service
            - Name: expenseClaimManagement-auth
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: expenseClaimManagement-auth
              ServiceKeyName: expenseClaimManagement-auth-key
              sap.cloud.service: expenseClaimManagement.service

resources:
  - name: expenseClaimManagement-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: expenseClaimManagement-${org}-${space}
        tenant-mode: dedicated
        oauth2-configuration:
          credential-types:
            - "binding-secret"
            - "x509"
          redirect-uris:
            - https://*~{app-api/app-uri}/**
        role-collections:
          - name: 'Employee (expenseClaimManagement ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.Employee'
          - name: 'Administrator (expenseClaimManagement ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.Administrator'
          - name: 'Finance (expenseClaimManagement ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.Finance'
          - name: 'Manager (expenseClaimManagement ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.Manager'
    requires:
      - name: app-api
  - name: expenseClaimManagement-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: expenseClaimManagement-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
    requires:
      - name: srv-api

  - name: expenseClaimManagement-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
  - name: expenseClaimManagement-html5-runtime
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-runtime
